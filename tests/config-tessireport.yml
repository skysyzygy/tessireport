default:
  tessiflow.email: ssyzygy@bam.org
# tessilake settings
  tessilake:
    depths:
      deep:
        path: !expr file.path(tempdir(),"deep")
      shallow:
        path: !expr file.path(tempdir(),"shallow")
        format: feather

