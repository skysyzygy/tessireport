test_that("read.duplicates_report loads the duplicates stream", {
  expect_equal(2 * 2, 4)
})

test_that("output.duplicates_report segments duplicates by customer number", {
  expect_equal(2 * 2, 4)
})

test_that("output.duplicates_report reorders and renames the columns", {
  expect_equal(2 * 2, 4)
})
test_that("output.duplicates_report dispatches to send_xlsx", {
  expect_equal(2 * 2, 4)
})
